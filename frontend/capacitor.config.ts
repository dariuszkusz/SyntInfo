import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.syntinfo.app',
  appName: 'SyntInfo',
  webDir: 'dist/frontend/browser'
};

export default config;
